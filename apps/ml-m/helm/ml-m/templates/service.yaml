apiVersion: v1
kind: Service
metadata:
  name: {{ include "ml-m.fullname" . }}
  labels:
    {{- include "ml-m.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ml-m.selectorLabels" . | nindent 4 }}
